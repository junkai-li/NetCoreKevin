启动前需确保 MySQL、Redis、可选的 Consul/RabbitMQ/Qdrant/Ollama 等外部依赖可用；环境变量通过 `EnvironmentConfigHelper.GetEnvironment()` 切换，对应 appsettings.Development.json / appsettings.Test.json；可通过 `dotnet publish -c Release` 发布到 FolderProfile 指定目录；容器化使用根目录下 Dockerfile。