开发启动使用 `npm run serve`（默认 development 模式）或 `npm run serve:pre` 切换预发环境；构建产物输出到 `dist/`，可通过 `npm run build` / `build:dev` / `build:pre` 指定模式；开发服务器通过 `vue.config.js` 将 `/VarApi` 反向代理到 `http://localhost:9901`，需确保后端服务在该端口运行。