集成测试通过 `WebApplicationFactory<WebApi.Program>` 启动 `App/WebApi` 服务，测试类中硬编码客户端地址为 `http://localhost:9901`，运行前需确保该端口未被占用且 WebApi 可正常启动；测试用例通过 `X-Forwarded-For` 与 `X-API-Version` 请求头注入上下文，并先调用 `AppGetToken.GetToken` 获取鉴权 Token。